name: Release
 
on:
  push:
    tags: ["v*"]
  workflow_dispatch:
 
jobs:
  stage:
    # @main always tracks latest. For stability, tag the .github repo (e.g. v1) and pin to @v1.
    uses: bitovi/.github/.github/workflows/npm-stage-publish.yml@main
    permissions:
      contents: read
      id-token: write       
